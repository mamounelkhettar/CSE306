
#include "scene.h"

#define STB_IMAGE_WRITE_IMPLEMENTATION
#include "stb_image_write.h"

int main(int argc, char *argv[]) {

    int H ; // height
    int W ; // width
    /*
    Scene scene ;
    std::vector<Vector> img ;

    // scan all pixels
    for (int i = 0; i < H ; i++) {
        for (int j = 0; j < W ; j++) {




        }

    }
    
    // generate the image
    stbi_write_png("image.png", W, H, 3, &img[0], 0) ;
    */
    return 0;
}